{
  "manifest_version": 1,
  "name": "username",
  "version": "1.0.0",
  "description": "Social/forum account discovery via Apify (Maigret): username → accounts across 3000+ sites. Opt-in / sensitive; never a default.",
  "entries": [
    {
      "kind": "source",
      "type": "username",
      "label": "Username OSINT (Maigret, Apify)",
      "summary": "Social/forum account discovery via Apify (Maigret): username → accounts across 3000+ sites. Opt-in / sensitive; never a default.",
      "env": [
        "APIFY_TOKEN"
      ],
      "base": [
        "bash",
        "shipped:providers/sources/username/username.sh"
      ],
      "needs": "APIFY_TOKEN",
      "timeoutMs": 360000,
      "doctor": {
        "check": "env_all",
        "env": [
          "APIFY_TOKEN"
        ],
        "okNote": "APIFY_TOKEN present (opt-in username→accounts OSINT — authorized use only)",
        "missingNote": "APIFY_TOKEN missing for username (Maigret account discovery) scans"
      },
      "refForms": [
        {
          "form": "username:<handle>"
        }
      ]
    }
  ]
}
