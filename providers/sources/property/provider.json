{
  "manifest_version": 1,
  "name": "property",
  "version": "1.0.0",
  "description": "Address → county assessor / tax / recorder records via Apify: owner / value / tax / sale history. Opt-in / sensitive; never a default.",
  "entries": [
    {
      "kind": "source",
      "type": "property",
      "label": "Property / assessor records (Apify)",
      "summary": "Address → county assessor / tax / recorder records via Apify: owner / value / tax / sale history. Opt-in / sensitive; never a default.",
      "env": [
        "APIFY_TOKEN"
      ],
      "base": [
        "bash",
        "shipped:providers/sources/property/property.sh"
      ],
      "needs": "APIFY_TOKEN",
      "timeoutMs": 360000,
      "doctor": {
        "check": "env_all",
        "env": [
          "APIFY_TOKEN"
        ],
        "okNote": "APIFY_TOKEN present (opt-in property/assessor records — authorized use only)",
        "missingNote": "APIFY_TOKEN missing for property (address→assessor/tax records) scans"
      },
      "refForms": [
        {
          "form": "property:<street, city, ST zip>"
        }
      ]
    }
  ]
}
